# postman-datadriven-api-testing
This project demonstrates data-driven REST API testing using Postman on the ReqRes public API. It validates an end-to-end user lifecycle including authentication, user creation, update, and deletion using environment variables, request chaining, and Postman test scripts.
